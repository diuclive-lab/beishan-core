# Claude 记忆导入 v2.5 — 将 Claude 记忆文件导入知识库
#
# 治理框架引用：
#   - 证据等级 → docs/ABSORPTION_GOVERNANCE.md §1

id: claude_memory_ingest
output_target: chat

steps:
  - id: import_memory
    plugin: claude_plugin
    type: claude_memory_import
    timeout: 30
    on_error: done
    inputs:
      name: "${input}"
