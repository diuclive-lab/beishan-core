# 测试右花调用工作流 v2.5
#
# 通过 L4 编排显式调用右花（不走 DeepSeek 首轮路由）
#
# 治理框架引用：
#   - 证据等级 → docs/ABSORPTION_GOVERNANCE.md §1

id: test_right_flower
output_target: chat
steps:
  - id: call_flower
    plugin: fake_example
    type: code.review
    timeout: 30
    on_error: done
    inputs:
      instruction: "${input}"
