# 开发日志 2026-05-25

## 缺口修复：三条雷 + 事件机制 + 对话持久化

### 背景

2026-05-24 的三次吸收暴露了三个真实漏洞。经过缺口分析（`openhuman_capability_map.md`），发现其中一些不是"设计决策"而是"没想到"。今日针对性修复。

### 修复项

| 雷 | 类型 | 改动 | 行数 |
|---|------|------|------|
| 检索评分归一化 | 遗漏 | knowledge.go ranking 阶段关键词 ×6 映射 | 9 |
| SESSION_EXPIRED 回退 | 遗漏 | code_security.go bytes.Contains 检测 | 6 |
| 事件机制 | 架构缺失 | observatory/events.go: PublishEvent + Subscribe | 150 |
| 对话持久化 | 架构缺失 | main.go agent.complete subscriber → conversations/ 目录 | 28 |

### 事件机制设计

方案 D：JSONL 持久化 + 单层 subscriber。事件即持久化，不分离。

```
observatory.PublishEvent(event)
  ├─ 写入 events_YYYYMMDD.jsonl（审计）
  └─ 同步通知所有匹配 subscriber

observatory.Subscribe("agent.*", handler)
  └─ 按事件名前缀匹配（"agent."匹配agent.spawn/complete/failed）
```

**事件类型**：
- `agent.spawn` — 子智能体启动（携带 agent_id + task_prompt）
- `agent.complete` — 子智能体完成（携带完整消息快照）
- `agent.failed` — 子智能体失败

**持久化**：`agent.complete` subscriber 将对话写入 `eval/run/conversations/{agent_id}_{timestamp}.json`，供回溯使用。

### 会话持久化示例

```json
{
  "agent_id": "researcher",
  "iterations": 1,
  "output": "24",
  "messages": [
    {"role": "assistant", "content": "24"}
  ]
}
```

### 设计纪律更新

新增 `DESIGN_PRINCIPLES.md` 章节「设计决策 vs 遗漏」：
- 不是所有"设计选择"都是故意的
- 先假设是遗漏，再证明是设计
- 3 行能修的不是决策
- 每个缺口必须有"原因 + 后果 + 补救"

### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `internal/observatory/events.go` | 新增 | 事件总线：Event 类型 + PublishEvent + Subscribe + InitEvents |
| `internal/agent/runner.go` | 修改 | 发布 agent.spawn/complete/failed 事件 + 消息快照 |
| `internal/tools/knowledge.go` | 修改 | 检索评分归一化（关键词 ×6） |
| `internal/tools/code_security.go` | 修改 | SESSION_EXPIRED 检测 + 自动回退 |
| `cmd/beishan/main.go` | 修改 | 事件初始化 + subscriber 注册 + 对话持久化 |
| `DESIGN_PRINCIPLES.md` | 修改 | 新增"设计决策 vs 遗漏"纪律 |
| `CHANGELOG.md` | 修改 | 完整的 2026-05-24 日志 |
| `DIRECTORY.md` | 修改 | agent/discovery/rightflower 包更新 |
| `README.md` | 修改 | 99 工具、子智能体委派、右花通车 |
| `docs/reports/openhuman_capability_map.md` | 修改 | 缺口分析更新：事件机制完成 |
| `.gitignore` | 修改 | 添加 eval/run/events/ conversations/ traces/ |
| `workflows/absorb_right_flower.yaml` | 修改 | Step 2.5 缺口分析格式强化 |

### 深度吸收：子智能体自动暴露为独立工具

吸收 OpenHuman 的 ArchetypeDelegationTool 模式——每个注册的子智能体自动生成一个 `delegate_to_{id}` L3 工具。

**效果**: LLM 可以看到的不是一个笼统的 `spawn_subagent`，而是一组有明确名称和描述的独立工具。

| 工具 | 说明 |
|------|------|
| `delegate_to_researcher` | 研究员（搜索工具） |
| `delegate_to_summarizer` | 摘要（文件读取工具） |
| `spawn_subagent` | 通用委派 |
| `spawn_parallel` | 并发委派 |

验证：delegate_to_researcher("13+13=?") → 26 ✅

### 核心认知

1. **"设计决策"是甩锅最常用的借口**。今天拆的三条雷里，评分归一化和 SESSION_EXPIRED 根本不是什么设计决策——就是没想到。诚实面对这一点比找一个漂亮的理由更重要。

2. **事件机制补齐了一个架构级短板**。之前所有"通知类"需求都被阻塞（embedding 进度、failover 通知、子智能体进度）。现在有了 `PublishEvent` + `Subscribe`，这类需求都有了基础。

3. **持久化和事件是一体两面**。不是先做事件再做持久化，而是事件本身就是持久化——每条事件都是不可变的状态记录。需要回溯时直接查事件日志即可。

---

## 今日总结

### 已完成

| 类别 | 项 | 状态 |
|------|------|------|
| 缺口修复 | 检索评分归一化（关键词 ×6 同尺度） | ✅ |
| 缺口修复 | SESSION_EXPIRED 自动回退规则检查 | ✅ |
| 架构补齐 | 事件总线 observatory.PublishEvent + Subscribe | ✅ |
| 架构补齐 | 子智能体对话持久化 conversations/ | ✅ |
| 深度吸收 | delegate_to_* 自动暴露为独立 L3 工具 | ✅ |
| AI 可读 | CLAUDE.md 全面升级（数据流/协议/guardrails） | ✅ |
| AI 入口 | README 顶部 AI 摘要 + 指向 CLAUDE.md | ✅ |
| CI 增强 | 文档一致性检查 4→9 项 | ✅ |
| 文档更新 | 全部关键文档 AI Summary + 设计纪律 | ✅ |

### 未完成（已知缺口）

| 缺口 | 原因 | 补救 |
|------|------|------|
| embedding 端点未配置 | Qwen 27B 太重 | 下次启用 gemma-4-E4B |
| LLM function calling | 依赖 provider 支持 | 条件成熟时升级 |
| 跨平台部署 | launchd 仅 macOS | 需 systemd / Docker |
| 事件 subscriber 仅日志 | 无自动化反应 | 未来按需添加 |

### 下一步思路

1. **配置轻量 embedding 模型**（gemma-4-E4B）— 启用语义搜索
2. **接新的右花项目** — 走完整吸收工作流验证可重复性
3. **事件 subscriber 实用化** — embedding 完成后自动通知
