# 开发日志 2026-05-26

## 全量 v2.5 YAML 工作流升级 + Go 工具反推 + 引擎修复

### 核心变更

本日涉及 45 个文件、+1494 / −951 行。覆盖：治理框架搭建、40 个工作流升级、Go 工具增强、引擎 bug 修复、本地模型更换。

---

### 一、治理根茎建立

创建 `docs/ABSORPTION_GOVERNANCE.md` 作为底座治理标准定义。

从外部 AI 的 v3 方案中提取了 5 个结构性改进注入 v2.5 工作流，摒弃了 Research Protocol 风格的重 meta 层：
- 证据等级 E1-E4（关键设计决策必须标注来源）
- 吸收等级 L0-L4（不是全吸或不吸，分级决策）
- 风险分类（correctness/compatibility/performance/security/operability/dependency）
- 升级策略（连续失败 >=2 次时降级/终止）
- 决策登记册 + 产出物清单

引用关系：
```
CLAUDE.md → docs/ABSORPTION_GOVERNANCE.md (治理标准)
          → docs/V25_WORKFLOW_STANDARD.md (YAML 编写标准)
          → workflows/*.yaml (引用治理标准节号)
```

### 二、v2.5 工作流升级（40/40 YAML）

分批升级完成。10 个核心 + 3 个深层 + 27 个浅层。

**Tier-1 Core（深度升级）：**

| YAML | 升级内容 |
|------|----------|
| absorb_right_flower | 14 步协议 + 证据等级 + L2/L3 分级 + escalation policy |
| code_deep_analyze | terminal → memory_plugin + evidence + risk_register + gap_analysis + runtime_anatomy |
| knowledge_review_scheduler | 来源追溯(session_id) + KB 查重 + 证据标注 |
| kb_hygiene_plus | on_error 兜底 + 治理引用 |

**Tier-2 Leverage（深度升级）：**

| YAML | 升级内容 |
|------|----------|
| code_review_9x | evidence 标注 + risk_register + 3 轮硬上限 |
| project_analyze | base_capability_inventory + evidence 标注 |
| knowledge_heal | 结构化报告 + evidence |
| file_ingest | provenance 追踪 + key_claims |
| content_review | source_credibility + quality_evidence + relevance_evidence |
| insight_followup | evidence + source_entry_id 追溯 |
| opensource_project_ingest | evidence 标注 + on_error |
| web_research_auto | 来源可信度 + 治理引用 |

**浅层升级（27 个）：** 治理引用 + on_error 兜底 + 统一的 header 格式。

### 三、Go 工具反推迭代（YAML 驱动 L2/L3）

| 工具 | 变更 | 来源 YAML | 受益 YAML 数 |
|------|------|-----------|-------------|
| code_tree | +list_files, +lang 参数 | code_deep_analyze | 5 |
| code_stats | +list_files, +limit 参数 | code_deep_analyze | 3 |
| go_struct_scan | +root(批量模式), +import_limit | code_deep_analyze | 2 |
| code_read_external | +paths(批量读取) | code_deep_analyze | 2 |
| base_capability_inventory | 新建 | project_analyze | 3 |

### 四、工作流引擎 Bug 修复

**问题：** `resolveJSONValue` 递归无深度限制，处理 `code_tree` 大 JSON 输出时栈溢出。

**根因：** engine.go:620-622 对 JSON 字符串做解包递归，嵌套的大 JSON 导致无限递归。

**修复：** 新增 `resolveJSONValueDepth(raw, depth int)`，depth > 10 时截断返回原始数据。

**影响：** 直接影响 `code_deep_analyze` Step 1（code_tree list_files）和 `quick_project_scan`（parallel + memory_plugin）。

### 五、Agent 系统修复（来自 P2 吸收缺口分析）

使用 v2.5 工作流对已吸收的 P2 Agent 委派做后验缺口分析，发现 7 个遗漏：

| # | 类别 | 严重度 | 修复 |
|---|------|--------|------|
| 1 | 并发安全 | critical | currentSpawnDepth 全局 int → context-based depth |
| 2 | 错误分类 | high | SubagentResult.Error string → ErrorCode 枚举 |
| 3 | 截断通知 | high | tool 结果截断时标注 [truncated: X/Y chars] |
| 4 | 输出校验 | medium | 检测空输出/放弃响应 |
| 5 | LLM 重试 | medium | 网络抖动自动重试 1 次 |
| 6 | 异步写盘 | medium | conversation persistence goroutine 异步 |
| 7 | 空 prompt | low | RunSubagent 入口前置校验 |

### 六、本地模型更换

| 模型 | 状态 | 用途 |
|------|------|------|
| ~~Qwen3.6-27B~~ | 已下线 | 之前太重导致超时 |
| gemma-4-E4B-it (4B, port 8090) | 已启用 | 日常分类/摘要推理 |
| gemma-4-31B-it (31B, port 8091) | 已启用 | API 故障切换（FailoverProvider） |

配置文件：`providers.json`，通过 `LLM_PROVIDERS_CONFIG` 环境变量加载。

### 七、实测数据

17/18 个工作流运行成功（94.4%），额外 20 次 LLM 调用，44,855 tokens。

**输出质量验证：**
- code_deep_analyze: evidence ×29, key_decisions ×4, risk_register ×1 ✅
- content_review: evidence ×7, source_credibility ×1, score fields ✅
- 8 个其他工作流通过 API 测试 ✅

### 新增文档

| 文档 | 内容 |
|------|------|
| docs/ABSORPTION_GOVERNANCE.md | 吸收治理框架（8 节） |
| docs/V25_WORKFLOW_STANDARD.md | v2.5 YAML 参考标准（强制项/条件项/禁止项/模板） |

### 文件统计

```
45 files changed, 1494 insertions(+), 951 deletions(-)
```

### 未完成

- 简单 YAML 的 evidence 要求增加了 token 消耗但收益有限 → 等待数据驱动决策
- `_template.yaml` 未升级（模板文件）
- 所有 YAML 需要长期运行确认稳定性

---

## OpenClaw 吸收工作流 v2.5 完整执行

### 执行概要

对 OpenClaw 右花完整走了一遍 `absorb_right_flower.yaml` v2.5（14 步）。

| Step | 结果 | 关键产出 |
|:----:|:----:|---------|
| 0 源码解剖 | ✅ | 架构图、数据流、设计决策（E2/E3） |
| 0.5 冲突扫描 | ✅ | 端口/env 无冲突，能力重叠良性 |
| 0.7 动态行为 | ✅ | 37s LLM 延迟是唯一瓶颈 |
| 1 全量探测 | ✅ | 4 方法全部正常，边界条件正确处理 |
| 2 深度评估 | ✅ | 4 条件全过 (S1=S4)，设计哲学对齐 |
| 2.5 缺口分析 | ✅ | 4 条缺口记录（含关联性风险） |
| 2.6 隐式假设 | ✅ | 发现 3 个隐藏假设（评估后均不需修复） |
| 2.7 演进预测 | ✅ | OpenClaw 活跃度极高，接口稳定 |
| 3 吸收决策 | ✅ | 全能力 L1 wrap，零内化 |
| 3.5 重写验证 | ⏭️ 跳过 | L1 wrap 不需重写 |
| 4 三层验证 | ✅ | L1/L2/L3 全过 + go test 全量回归 |
| 5 广度检查 | ✅ | 注册表无冲突，调用链完整 |
| 6 文档化 | ✅ | 已有文档覆盖完整 |

### 吸收等级

| 能力 | 等级 | 说明 |
|------|:----:|------|
| agent.chat | L1 wrap | 保持 adapter 包装 |
| tool.execute | L1 wrap | 保持 adapter 包装 |
| skills.list | L1 wrap | 保持 adapter 包装 |
| gateway.status | L1 wrap | 保持 adapter 包装 |

### 缺口清单

| 未吸收能力 | 原因 | 补救 |
|-----------|------|------|
| 多通道消息 (20+) | 冲突 — 右花职责 | internal/channels 余量接口 |
| ClawHub 技能市场 | 太复杂 | 通过协议调 skills.list |
| 安全策略链 | 太复杂 + 重叠 | 硬化层已覆盖核心 |
| agent.chat 深层集成 | 不值得 | 日常用 think_plugin |

### 最终判定

**CERTIFIED** — OpenClaw 右花可进入生产体系使用。

---

## Embedding 服务配置完成

### 模型

- 模型: nomic-embed-text-v1.5 (Q4_K_M)
- 大小: 79 MB
- 向量维度: 768
- 服务: `llama-server` on `:8090`
- 启动: `llama-server --embeddings --model /Users/dc/models/nomic-embed/nomic-embed-text-v1.5.Q4_K_M.gguf --port 8090`

### 底座接入

通过环境变量激活语义搜索：
- `EMBEDDING_ENDPOINT=http://localhost:8090/v1/embeddings`
- `EMBEDDING_MODEL=nomic-embed-text-v1.5`

知识重索引工作流已触发，现有知识条目的语义向量正在计算中。

---

## Embedding 语义搜索配置完成

### 模型部署
- 模型: nomic-embed-text-v1.5 (Q4_K_M, 79MB)
- 向量维度: 768
- 服务: llama-server on :8090
- 启动命令:
  `llama-server --embeddings --pooling mean --model /Users/dc/models/nomic-embed/nomic-embed-text-v1.5.Q4_K_M.gguf --port 8090`

### 底座接入
- `EMBEDDING_ENDPOINT=http://localhost:8090/v1/embeddings`
- `EMBEDDING_MODEL=nomic-embed-text-v1.5`
- 已写入 ~/.zshrc 永久生效
- 支持环境变量: EMBEDDING_API_KEY（可选）

### 提升效果
语义搜索将 knowledge search 从 L0（关键词匹配）升级为 L0 + L1（语义匹配）：

| 对比 | 之前（纯关键词） | 现在（+向量语义） |
|------|----------------|-----------------|
| 示例 | 搜"并发模型"→ 不含此词的知识不出现 | 搜"并发模型"→ "goroutine channel"也能找到 |
| 机制 | 子串/分词匹配 | 768 维向量余弦相似度 |
| 覆盖 | 字面匹配 | 语义相近即可 |
| 模型 | 无 | 79MB 本地模型，M2 秒级推理 |

### 永久配置
```bash
# ~/.zshrc
export EMBEDDING_ENDPOINT=http://localhost:8090/v1/embeddings
export EMBEDDING_MODEL=nomic-embed-text-v1.5
```
