# 开发日志 2026-05-28

## 概要

系统性代码审查第二期。本次对 Plugin 层和 Workflow YAML 层做全面合规审查并按 §6.1/§6.3 清单逐项核对修复。

不增加功能，专注修复遗漏的细节。

---

## 任务 A：Plugin 层审查（11 个文件）

按 `docs/CODE_REVIEW_SPEC.md §6.1` 的五项检查清单逐条核对。

### 发现的问题

| 问题类型 | 涉及文件 | 严重程度 |
|----------|----------|:--------:|
| 成功路径返回空 Message（Payload=nil） | browser_plugin, image_gen_plugin, tts_plugin, write_plugin (4 cases) | **高** — 调用方收不到结果 |
| ValidateAndExecute 后未检查 result.Success | browser_plugin, image_gen_plugin, tts_plugin, write_plugin, search_plugin (web_fetch), skill_factory (skill_evaluate) | **中** — 失败时返回误导性 `.result` 类型 |
| 用 `result.Error != ""` 代替 `!result.Success` | notify_plugin | **低** — 语义不等价 |
| 失败时返回 `.result` 类型而非 `.error` | todo_plugin, skill_factory (skill_evaluate), search_plugin (fallback), notify_plugin (已修复) | **中** — 调用方无法区分成功/失败 |

### 修复汇总

**browser_plugin.go / image_gen_plugin.go / tts_plugin.go：**
- 之前：`result := tools.ValidateAndExecute(...)` → `return kernel.Message{}, nil`
- 之后：`!result.Success` 检查 + 编码 Output 为 JSON Payload + 返回 `msg.Type+".result"`

**write_plugin.go：**
- write_file/read_file/search_files/patch 四个 case 从空 Message 改为输出 payload
- 全部 case 添加 `!result.Success` 检查

**notify_plugin.go：**
- `result.Error != ""` → `!result.Success`（两者语义不同）

**search_plugin.go：**
- web_fetch/web_extract/web_render 添加 `!result.Success` → `.error` 类型
- web_search 降级路径添加 `.error` 类型判断

**todo_plugin.go + skill_factory_plugin.go：**
- 失败时返回 `msg.Type+".error"` 而非 `.result`

### 未发现问题的插件

| 文件 | 说明 |
|------|------|
| clause_analyzer_plugin.go | 不调 ValidateAndExecute，Payload 解析有双重回退，default 返回 error |
| legal_write_plugin.go | 同上 |
| legal_search_plugin.go | 4 处 ValidateAndExecute 全部检查了 result.Success |

---

## 任务 B：Workflow 层 v2.5 合规扫描（44 个文件）

按 `docs/CODE_REVIEW_SPEC.md §6.3` + `docs/V25_WORKFLOW_STANDARD.md` 检查。

### 检查结果

| 检查项 | 修复前 | 修复后 |
|--------|:-----:|:------:|
| 顶部有注释（用途/触发） | ✅ 全部有 | ✅ |
| id 与文件名一致 | ✅ 全部一致 | ✅ |
| 每步有 timeout | 2 文件缺 | ✅（排除 absorb_right_flower 非标准格式） |
| 每步有 on_error | 10 文件缺 | ✅（排除 absorb_right_flower 非标准格式） |
| output_target 声明 | 4 文件有 | ✅ 43/43 文件有 |
| 不使用 provider:local | ✅ 无违规 | ✅ |

### 修复的 11 个文件

**缺失 on_error 的步骤：**

| 文件 | 修复 |
|------|------|
| code_audit.yaml | scan_metrics/diagnose/done 添加 on_error |
| code_deep_analyze.yaml | 8 个步骤添加 on_error（static_metrics/runtime_anatomy/select_files/read_files/analyze_files/synthesize/save_knowledge/done） |
| code_review_9x.yaml | parallel_review 添加 on_error: report, report 添加 timeout: 30 |
| file_ingest.yaml | save 添加 on_error: done |
| kb_hygiene_plus.yaml | notify_done 添加 on_error: done |
| knowledge_review_scheduler.yaml | report_empty 添加 on_error: done |
| opensource_project_ingest.yaml | research_parallel 添加 on_error: analyze |
| project_analyze.yaml | 8 个步骤添加 on_error |
| quick_project_scan.yaml | parallel_scan 添加 on_error: summarize |
| security_audit_parallel.yaml | parallel_audit 添加 on_error: cross_check |

**output_target 声明：** 37 个文件缺失，全部添加 `output_target: chat`

### 特殊处理

`absorb_right_flower.yaml` 使用非标准步骤格式（`title/deliverables/checks` 而非 `plugin/type/timeout/on_error`），是参考文档型工作流，不可由引擎执行。已在 `docs/KNOWN_LIMITATIONS.md §15` 登记。

---

## 新增文档更新

| 文档 | 变更 |
|------|------|
| `docs/CODE_REVIEW_SPEC.md §9` | 新增 6 种 AI 高频错误模式（空 Message 返回、Success 未检、Error≠Success、on_error/timeout 缺失、output_target 缺失、非标准 YAML 格式）|
| `docs/KNOWN_LIMITATIONS.md` | 新增 §15：absorb_right_flower.yaml 是不可执行的参考文档 |

---

## 全量验证

```
go build ./...          ✅
go test ./...           ✅ 21 packages
integration_check.sh    ✅
```

---

## 文件统计

```
 8 plugin files  changed   (+encoding/json imports + Success checks + payload encoding)
11 workflow files changed  (+on_error fields)
41 workflow files changed  (+output_target: chat)
 2 doc files      changed  (CODE_REVIEW_SPEC.md §9, KNOWN_LIMITATIONS.md §15)
────────────────────────────────────────────────────────
~62 files changed
```

---

## 新发现的 AI 错误模式（已更新至 §9）

1. **Plugin 返回空 Message** — `return kernel.Message{}, nil` 使调用方收不到结果。检测：搜索 `return kernel.Message{}, nil` + 非 default 分支。
2. **ValidateAndExecute 后未检查 result.Success** — 语法正确但语义不完备。检测：搜索 ValidateAndExecute 后面是否有 .Success 引用。
3. **用 `result.Error != ""` 代替 `!result.Success`** — ErrorResult 同时设置两者，但自定义 handler 可能只设 Success=false。
4. **workflow YAML 缺少 on_error/timeout** — v2.5 强制项，漏了可能使工作流挂死。检测：grep 对比步骤数量与 on_error/timeout 数量。
5. **workflow YAML 缺少 output_target** — 引擎默认 chat，但不声明违反规范。检测：grep -L output_target。
6. **非标准步骤格式 YAML 混入 workflows/** — 引擎会执行空 plugin 步骤，静默失败。检测：检查 step 的 plugin 字段是否存在。
