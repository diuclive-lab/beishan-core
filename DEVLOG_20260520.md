# 开发日志 2026-05-20

## 项目定位

经过多轮讨论，最终确定项目定位：

**Deterministic Personal Knowledge Runtime**

不是：
- AI Memory Framework
- Agent Cognitive Architecture
- 通用 agent memory 系统

而是：
- Personal Knowledge OS with Hardened Retrieval
- 确定性 > 潜在智能度
- 真实需求 > 架构完美性

---

## 核心设计原则

| 原则 | 说明 |
|------|------|
| 硬化层优先 | 能用代码写的逻辑绝不写在提示词里 |
| 工具 > Agent | 检索是写死在代码里的，不是 LLM 决定的 |
| LLM 只负责生成 | LLM 被压到 terminal renderer 角色 |
| 确定性检索 | SearchMemory 是确定性代码，不是 LLM 决策 |
| 有界注入 | 只在 think_plugin 注入，不污染其他插件 |
| 可审计 | 检索过程可追踪，写入需用户确认 |

---

## 方案探索过程（5 轮推演）

### 第 1 轮：硬化检索 + 自动注入（废弃）

**方案**：每次聊天自动调用 KnowledgeRetrieve（BOW + 类型权重），结果注入 system prompt。

**问题**：
- L4（think_plugin）直接 import L3（internal/tools），突破架构边界
- 将判断权交给了 LLM，违反硬化层原则
- 知识注入后无法验证 LLM 是否真的使用

**结论：废弃。**

### 第 2 轮：全局 Context Enrichment Layer（否定）

**方案**：在 L1 Router 之后、L4 Plugin 之前，加一个全局富化层，所有消息都自动检索知识。

**推演问题**：
- workflow_plugin、terminal_plugin、todo_plugin 完全不需要知识
- "帮我跑一下开源雷达" → 检索到 agent 设计模式 → 干扰执行
- "你好"、"现在几点" 也要多一次 knowledge_search → 增加延迟

**结论：否定。** memory 应该按 runtime role 注入，不是全局 middleware。

### 第 3 轮：图结构神经网络（否定）

**方案**：L3 建知识图谱（带类型的关系边），L4 消费。

**推演问题**：
- L4 不会主动消费这个神经网络
- L3 建了一张死图，没有智能度
- 本地知识库并不比 API 优秀

**结论：否定。** 核心认知：本地知识库的优势从来不是"智能"，而是精确的、个人的、确定性的事实。

### 第 4 轮：插件级自动注入（采纳）

**方案**：只在 think_plugin 的 OnMessage 中，在调 LLM 之前固定执行一次知识检索，结果注入 `<background>` 标签。

```
think_plugin.OnMessage:
  用户消息
    → 1. SearchMemory(query)     ← 固定步骤，不是 LLM 决策
    → 2. FormatForPrompt(top-3) ← 确定性格式化
    → 3. callLLM(msg + background) ← LLM 只做生成
```

**与原则对账：**

| 原则 | 是否违反 |
|---|---|
| 硬化层原则 | ✅ 检索是确定性 L3 工具调用 |
| 工具 > Agent | ✅ 检索是写死在代码里的 |
| 提示词只描述格式 | ✅ system prompt 只多了知识内容 |
| 内核冻结 | ✅ 不动 L1 |
| 厂商无关 | ✅ embedding 通过环境变量配置 |
| 避免递归 | ✅ 用 llm.ChatCompletion，不走 think_plugin 路径 |

**结论：采纳，开始实现。**

### 第 5 轮：双链知识图谱（采纳）

在检索上线后，进一步发现：即使 Agent 会自动检索了，每条知识仍是孤立的。

**方案**：激活 KnowledgeEntry 已有的 Links 字段，实现：

1. **入库自动建链**：新条目入库后异步扫描已有条目，基于标签/主题/关键词重叠自动双向写入 Links
2. **图遍历检索**：SearchMemory 在直接命中后，沿 Links 做一跳扩展，二跳条目降权 50% 后一起注入

**结论：采纳。**

---

## 自主学习方案讨论

### 核心问题

是否增加智能体的自主学习能力？

### 讨论过程

**方案 A：自动写入（否定）**

对话结束自动判断是否值得记住。

**问题**：
- 自动写入比自动检索危险得多
- 错误知识 → 入库 → autoLink → embedding → graph expand → retrieval → 强化污染
- 形成正反馈污染环
- 违反确定性原则

**结论：否定。**

**方案 B：Suggest-to-Remember（采纳）**

Agent 检测 → 提议 → 明确确认 → knowledge_remember。

**流程**：
```
think_plugin
回答完成
↓
DetectCandidateKnowledge()
↓
candidate found
↓
"你似乎刚验证了一个稳定结论：
『M2 8GB 本地模型路线暂停』
是否加入知识库？"
↓
confirm
↓
knowledge_remember(...)
```

**优点**：
- 显式：用户看到提议，明确确认
- 有界：只在 think_plugin，不污染其他
- 可审计：用户知道什么被提议
- 确定性：触发条件是代码，不是 LLM
- 可回滚：没确认就不写

**结论：采纳。**

**方案 C：Scheduler Knowledge Review（采纳，但降级）**

定时审查对话，生成报告（不自动写入）。

**原方案**：scheduler → session_search → think_plugin analyze → report

**问题**：
- LLM pipeline nesting
- prompt drift
- extraction instability

**降级方案**：先做 deterministic review（基于关键词触发），不做 LLM 分析。

**结论：采纳，但先做 deterministic review。**

---

## 最终确定的三个 Stage

### Stage 1: Retrieval Trace

**目的**：可观测性，知道检索过程发生了什么。

**实现**：
- `RetrievalTrace` 结构体，context-style collector
- `SearchMemory` 添加 trace 参数
- think_plugin 打印到日志

**输出示例**：
```
[retrieval] query="本地模型方案怎么样了？" stages=2
  stage=keyword_expand method=llm_expand+score output=map[candidates:6 keywords:[本地模型方案 本地模型 local model 方案进展 调研]]
  stage=ranking method=sort+limit output=map[final:3]
```

### Stage 2: Suggest-to-Remember

**目的**：主动提议记住，用户确认后才写入。

**实现**：
- `PendingRemember` 结构体，TTL 10 分钟
- 触发关键词：已放弃、最终方案、确认结论、架构决定、经验教训、踩坑记录、最终决定
- think_plugin 检测触发词，提议记住
- 用户回复"确认"，调用 knowledge_remember

**流程**：
```
用户: "本地模型方案最终决定放弃了吗？"
Agent: "是的...已放弃..."
Agent: "是否将此结论加入知识库？回复「确认」即可。"
用户: "确认"
Agent: "已记录：最终决定"
```

### Stage 3: Scheduler Knowledge Review

**目的**：定时审查对话，提取值得记住的知识候选。

**实现**：
- 工作流 `knowledge_review_scheduler`
- 基于关键词触发（deterministic review）
- 只生成报告，不自动写入
- 人工确认后才入库

**流程**：
```
scheduler 定时触发
  ↓
扫描最近对话记录
  ↓
提取包含触发关键词的对话
  ↓
生成审查报告
  ↓
人工确认
  ↓
knowledge_remember
```

---

## 实现阶段路线

| 阶段 | 内容 | 状态 |
|------|------|------|
| Stage 1 | Retrieval Trace | ✅ 已完成 |
| Stage 2 | Suggest-to-Remember | ✅ 已完成 |
| Stage 3 | Scheduler Knowledge Review | ✅ 工作流已创建 |
| Stage 4 | 半自动批量确认 | 待定 |
| Stage 5 | 谨慎考虑 autonomous write | 待定 |

---

## 关键设计决策

### 决策 1：不做自动写入

**原因**：自动写入会导致正反馈污染环，违反确定性原则。

**替代方案**：Suggest-to-Remember，用户确认后才写入。

### 决策 2：不做 LLM 分类

**原因**：LLM-driven semantic ontology 会侵蚀 precision、determinism、auditability。

**替代方案**：代码化的触发关键词，确定性检测。

### 决策 3：先做 autonomous discovery，不做 autonomous mutation

**原因**：发现值得记住的内容（discovery）比自动写入（mutation）安全得多。

**路线**：Retrieval Trace → Suggest-to-Remember → Scheduler Review → 半自动确认 → 谨慎考虑自动写入。

### 决策 4：PendingRemember 使用 TTL

**原因**：防止"今天 suggestion，明天确认"污染 session 状态。

**实现**：TTL 10 分钟，过期自动清理。

### 决策 5：触发关键词保守化

**原因**：宁可少建议，不要疯狂 Suggest-to-Remember。

**实现**：只检测明确的结论标记：已放弃、最终方案、确认结论、架构决定、经验教训、踩坑记录、最终决定。

---

## 文件清单

| 文件 | 改动 |
|------|------|
| `kernel/msg.go` | ~~+SessionID 字段~~ → 已还原（架构回归） |
| `main.go` | session_id 注入到 Payload（而非 Message struct） |
| `internal/tools/knowledge.go` | +RetrievalTrace 结构体，SearchMemory 添加 trace 参数 |
| `plugins/think_plugin.go` | +Suggest-to-Remember 逻辑，+session-scoped PendingRemember，+extractSessionID |
| `web/index.html` | +lastSessionId 状态维持，聊天请求携带 session_id |
| `workflows/knowledge_review_scheduler.yaml` | 新增知识审查工作流 |
| `DEVLOG_20260520.md` | 本开发日志 |

---

## 关键实现细节：Session-Scoped PendingRemember

### 问题发现

初始实现中 PendingRemember 是全局的（单变量），存在两个问题：

1. **跨会话碰撞**：多个用户同时使用时，后一个会话会覆盖前一个会话的 pending remember
2. **HTTP 无状态**：每次 HTTP 请求生成新的 sessionID，导致"确认"请求找不到之前的 pending

### 解决方案

**Step 1: 添加 SessionID 到 Message（L1 改动）**

```go
// kernel/msg.go
type Message struct {
    ...
    SessionID string `json:"session_id,omitempty"` // 会话 ID，用于 request-scoped 状态
}
```

**讨论**：这是 L1 kernel 改动。SessionID 是请求级元数据，与 ReplyTo、CorrelationID 同级。虽然可以放在 Payload 里，但语义上更清晰。用户最终接受此方案，但要求以后动 L1 前必须多次提醒确认。

**Step 2: HTTP handler 支持客户端传入 session_id**

```go
// main.go
sessionID := ""
if sid, ok := raw["session_id"].(string); ok && sid != "" {
    sessionID = sid
}
if sessionID == "" {
    sessionID = newSessionID()
}
```

**Step 3: 响应包装附加 session_id**

```go
wrapped := map[string]interface{}{
    "session_id": sessionID,
    "sender":     resp.Sender,
    "type":       resp.Type,
    "payload":    json.RawMessage(resp.Payload),
}
json.NewEncoder(w).Encode(wrapped)
```

**Step 4: Session-scoped PendingRemember**

```go
// plugins/think_plugin.go
var (
    pendingRemembers   = make(map[string]*PendingRemember)  // sessionID -> PendingRemember
    pendingRemembersMu sync.Mutex
)

func createPendingRemember(sessionID, title, summary string) *PendingRemember {
    pendingRemembersMu.Lock()
    pendingRemembers[sessionID] = pr // 每个 session 只保留一个 pending
    pendingRemembersMu.Unlock()
    return pr
}
```

**Step 5: Web UI 维持 session 状态**

```javascript
// web/index.html
let lastSessionId = null;

// 发送请求时携带上次的 session_id
if (lastSessionId) {
    payload.session_id = lastSessionId;
}

// 响应中存储 session_id
if (d.session_id) {
    lastSessionId = d.session_id;
}
```

---

## 测试结果

| 测试 | 结果 |
|------|------|
| Retrieval Trace 输出 | ✅ 正常（服务器控制台可见） |
| Suggest-to-Remember 触发 | ✅ 正常（触发词"最终方案"命中） |
| 确认流程写入知识库 | ✅ 正常（session_id 传递成功） |
| 无 session_id 确认 | ✅ 正常（走普通 LLM 流程） |
| 错误 session_id 确认 | ✅ 正常（走普通 LLM 流程） |
| Scheduler Review 工作流 | ✅ 已创建（待测试） |

### 测试命令

```bash
# 1. 触发 Suggest-to-Remember
curl -s http://localhost:8013/api/chat \
  -d '{"recipient":"think_plugin","type":"chat","payload":{"message":"本地模型方案最终方案确定了吗？"}}' \
  | grep -o '"session_id":"[^"]*"'

# 2. 用同一个 session_id 确认
curl -s http://localhost:8013/api/chat \
  -d '{"session_id":"<上一步的session_id>","message":"确认"}'
```

---

## 后续方向

### 短期
- Stage 3 工作流测试
- scheduler 定时触发配置

### 中期
- Stage 4 半自动批量确认
- retrieval trace 可视化（web debug panel）

### 远期
- Stage 5 谨慎考虑 autonomous write
- 多端同步：知识库跨设备共享

---

## L1 改动讨论记录

### 问题

实现 Suggest-to-Remember 的 session-scoped PendingRemember 时，需要在 Message 中传递 sessionID。有两种方案：

| 方案 | 优点 | 缺点 |
|------|------|------|
| Message.SessionID（L1 改动） | 语义清晰，与其他元数据一致 | 修改了冻结的 L1 |
| Payload 内嵌 session_id | 不动 L1 | 语义别扭，混在业务数据里 |

### 第一次决策（错误）

采用 Message.SessionID 方案，理由是"与 CorrelationID/ReplyTo 同级"。

### 架构反思

外部分析指出关键问题：
- 如果"与 CorrelationID 同级"就能加，那 UserID、TraceID、RequestID 都能用同样理由加进去
- L1 会膨胀为"万能 envelope layer"

**重新判断标准**：
- L1 只包含 kernel routing 必需字段（kernel 必须知道才能 deliver message）
- CorrelationID ✅（Call 配对）
- ReplyTo ✅（回程分派）
- SessionID ❌（kernel 不需要知道 session 才能 route message）

### 最终决策（正确）

采用 Payload 内嵌方案，还原 L1。

**实现**：
```go
// main.go - session_id 注入到 Payload
payloadObj := map[string]string{"message": txt, "session_id": sessionID}

// think_plugin.go - 从 Payload 提取
func extractSessionID(payload []byte) string {
    var obj map[string]interface{}
    if json.Unmarshal(payload, &obj) == nil {
        if sid, ok := obj["session_id"].(string); ok {
            return sid
        }
    }
    return ""
}
```

### 新增规则

1. **修改 L1 kernel 代码前必须多次提醒用户确认，不能直接修改。**
2. **L1 只包含 kernel routing 必需字段，不以"方便关联"为理由扩展。**

---

## Stage 3 工作流测试与修复（18:00-20:30）

### 测试过程

1. **启动 beishan-core 服务器**
2. **调用 Stage 3 工作流**：`POST /api/chat` with `workflow_run`
3. **发现问题**：scan 步骤输出为空，analyze 步骤未执行

### 发现的 Bug 及修复

#### Bug 1：session_search_plugin 返回空 Message

**现象**：工作流 scan 步骤执行成功，但输出为空。

**原因**：`session_search_plugin.go` 的 `OnMessage` 函数返回空的 `kernel.Message{}`，没有包含搜索结果。

**修复**：
```go
// 修复前
return kernel.Message{}, nil

// 修复后
return kernel.Message{
    Type:    msg.Type + ".result",
    Payload: []byte(fmt.Sprintf("%q", result.Output)),
}, nil
```

**文件**：`plugins/session_search_plugin.go`

---

#### Bug 2：workflow YAML 缺少 next 字段

**现象**：scan 步骤执行后直接结束，未执行 analyze 步骤。

**原因**：`knowledge_review_scheduler.yaml` 中 scan 步骤没有定义 `next` 字段，workflow engine 默认跳到 "done"。

**修复**：
```yaml
# 修复前
- id: scan
  plugin: session_search_plugin
  type: session_search
  ...

# 修复后
- id: scan
  plugin: session_search_plugin
  type: session_search
  ...
  next:
    - goto: analyze
```

**文件**：`workflows/knowledge_review_scheduler.yaml`

---

#### Bug 3：session_search schema 字段名错误

**现象**：scan 步骤报错 "参数校验不通过: [schema] 未知字段: query"

**原因**：workflow YAML 中使用 `query` 字段，但 session_search 工具定义的是 `keyword` 字段。

**修复**：
```yaml
# 修复前
inputs:
  query: "已放弃 最终方案 确认结论 架构决定 经验教训 踩坑记录 最终决定"

# 修复后
inputs:
  keyword: "已放弃 最终方案 确认结论 架构决定 经验教训 踩坑记录 最终决定"
```

**文件**：`workflows/knowledge_review_scheduler.yaml`

---

#### Bug 4：schema 不允许额外字段（session_id 注入失败）

**现象**：HTTP handler 自动注入 `session_id` 到 payload，导致 schema 验证失败。

**原因**：`ValidateParams` 函数强制检查所有字段必须在 `properties` 中定义，忽略 `additionalProperties` 设置。

**修复**：
```go
// 修复前
for key := range params {
    if props != nil {
        if _, ok := props[key]; !ok {
            return fmt.Errorf("[schema] 未知字段: %s", key)
        }
    }
}

// 修复后
additionalProperties, _ := schemaObj["additionalProperties"].(bool)
if !additionalProperties {
    for key := range params {
        if props != nil {
            if _, ok := props[key]; !ok {
                return fmt.Errorf("[schema] 未知字段: %s", key)
            }
        }
    }
}
```

**文件**：`internal/tools/schema_registry.go`

---

#### Bug 5：session_search 搜索到 workflow 执行结果

**现象**：scan 步骤搜索到的是 workflow 的执行结果，而不是用户对话。

**原因**：session_search 搜索所有 session 的所有消息，包括 workflow_plugin 的输出。

**修复**：
```go
// 添加过滤逻辑
if roleFilter != "" && m.Role != roleFilter {
    continue
}
// 排除 workflow 执行结果
if m.Role == "workflow_plugin" || m.Type == "workflow.result" {
    continue
}
```

**文件**：`internal/tools/session_search.go`

---

### 最终测试结果

```bash
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"type":"workflow_run","sender":"user","recipient":"workflow_plugin","payload":{"workflow":"knowledge_review_scheduler"}}'
```

**输出**：
```json
{
  "WorkflowID": "knowledge_review_scheduler",
  "Steps": [
    {"ID": "scan", "Output": "搜索结果..."},
    {"ID": "analyze", "Output": "[Knowledge Review Report]\n候选知识: 2\n..."}
  ],
  "FinalStep": "done",
  "Success": true
}
```

**功能验证**：
- ✅ scan 步骤正确搜索对话记录
- ✅ analyze 步骤正确执行，生成知识候选报告
- ✅ 过滤 workflow 执行结果，只搜索用户对话
- ✅ Schema 验证支持 additionalProperties

---

### 修改的文件清单

| 文件 | 改动 |
|------|------|
| `plugins/session_search_plugin.go` | 返回搜索结果（而非空 Message） |
| `workflows/knowledge_review_scheduler.yaml` | 添加 next 字段，修复 keyword 字段名 |
| `internal/tools/session_search.go` | 添加 additionalProperties，添加 role/workflow 过滤 |
| `internal/tools/schema_registry.go` | 支持 additionalProperties 字段 |

---

## 架构审查修复（20:30-21:00）

根据外部审查报告，修复了 3 个 bug + 1 个设计断裂。

### Bug 1：`${scan}` 变量引用永远不会解析 🔴

**问题**：workflow YAML 中 `${scan}` 引用不存在，engine 存储的是 `steps.scan.output`。

**修复**：
```yaml
# 修改前
${scan}

# 修改后
${steps.scan.output}
```

**文件**：`workflows/knowledge_review_scheduler.yaml`

---

### Bug 2：`callStructuredLLM` 的 system/user 提示词反了 🔴

**问题**：整个 prompt 被当成 system message，user message 为空。

**修复**：
```go
// 修改前
raw, err := llm.ChatCompletion(prompt, "", 120*time.Second)

// 修改后
raw, err := llm.ChatCompletion(
    "你是知识提取助手。严格按用户要求输出JSON，不要输出任何其他文本。",
    prompt,
    120*time.Second,
)
```

**文件**：`plugins/think_plugin.go`

---

### Bug 3：Session ID 断裂 — 设计决策 🔴

**问题**：Workflow 和确认流程在不同执行域，session_id 无法传递。

**决策**：采用**方案 A（文件持久化）**，符合确定性原则。

**实现**：
- 审查报告写入 `~/.hermes/reviews/` 目录
- 确认时从文件读取（而非内存）
- 格式：`确认 reviewID 1,2`

**文件**：`plugins/think_plugin.go`

---

### Bug 4：Workflow 返回格式不友好

**问题**：返回嵌套的 WorkflowResult JSON，用户不可读。

**修复**：直接返回 FinalOutput（可读文本），降级时才返回完整 JSON。

**文件**：`plugins/workflow_plugin.go`

---

### Bug 5：`validateReview` 在 `parseErr != nil` 时仍被调用

**问题**：JSON 解析失败时，对空结构体做无意义校验。

**修复**：先检查 parseErr，再校验 schema。

**文件**：`plugins/think_plugin.go`

---

### 最终测试结果

```bash
# 1. 运行工作流
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"type":"workflow_run","sender":"user","recipient":"workflow_plugin","payload":{"workflow":"knowledge_review_scheduler"}}'

# 输出：友好格式的审查报告，包含报告 ID
# [Knowledge Review Report]
# 候选知识: 2
# ...
# 报告 ID: review_1779281108490
# 回复「确认 review_1779281108490 1,2」即可入库

# 2. 确认入库
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"确认 review_1779281108490 1,2"}'

# 输出：已记录 2 条知识
```

**功能验证**：
- ✅ scan 步骤正确搜索对话记录（排除 workflow 结果）
- ✅ analyze 步骤使用正确的 system/user prompt
- ✅ 变量引用 `${steps.scan.output}` 正确解析
- ✅ 审查报告写入文件（`~/.hermes/reviews/`）
- ✅ 确认流程从文件读取，支持跨 session
- ✅ 返回格式友好（直接可读文本）

---

### 新增/修改的文件清单

| 文件 | 改动 |
|------|------|
| `workflows/knowledge_review_scheduler.yaml` | 修复变量引用 `${scan}` → `${steps.scan.output}` |
| `plugins/think_plugin.go` | 修复 LLM 调用参数、添加文件持久化、修复 validateReview 时序 |
| `plugins/workflow_plugin.go` | 优化返回格式（直接返回 FinalOutput） |

---

## 路线推进：从半截工程到完整 Pipeline（21:00-21:30）

### 路线 1：修 scan 精度（OR 匹配）

**问题**：session_search 的关键词匹配是整串匹配，不是 OR 匹配。

**修复**：
```go
// 修改前
if strings.Contains(strings.ToLower(m.Payload), strings.ToLower(keyword))

// 修改后
keywords := strings.Fields(strings.ToLower(keyword))
for _, kw := range keywords {
    if strings.Contains(payloadLower, kw) {
        matched = true
        break
    }
}
```

**文件**：`internal/tools/session_search.go`

**效果**：现在 `"已放弃 最终方案"` 会匹配包含"已放弃"**或**"最终方案"的消息。

---

### 路线 2：添加自然语言入口

**实现**：在 think_plugin 中添加确定性意图识别（不走 LLM 判断）。

| 用户输入 | 动作 |
|---------|------|
| `"审查一下最近对话"` / `"知识审查"` | 触发 `knowledge_review_scheduler` 工作流 |
| `"审查队列"` / `"待审查报告"` | 展示所有待确认报告 |
| `"确认 reviewID 1,2"` | 确认入库（已有） |

**关键修改**：
- ThinkPlugin 结构体添加 `Kernel *kernel.Kernel` 字段
- main.go 注册时传入 Kernel 引用
- 添加 `isReviewTrigger` / `isListReviews` 意图识别函数
- 添加 `triggerReviewWorkflow` / `handleListReviews` 处理函数

**文件**：
- `plugins/think_plugin.go`
- `main.go`

---

### 路线 3：接入 scheduler 定时触发

**实现**：调用已有的 scheduler_plugin，添加 cron 定时任务。

```bash
# 添加每天 22:00 的知识审查
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"type":"schedule_add","sender":"user","recipient":"scheduler_plugin","payload":{"cron":"0 22 * * *","workflow":"knowledge_review_scheduler","name":"daily_review"}}'
```

**日志确认**：
```
[调度] 添加定时任务: daily_review → cron 0 22 * * * 执行 knowledge_review_scheduler
[调度] cron daily_review → 下次触发: 22:00:00 (还有 1h5m12s)
```

---

### 完整 Pipeline 测试

```bash
# 1. 触发审查（自然语言）
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"审查一下最近对话"}'

# 输出：
# [Knowledge Review Report]
# 候选知识: 5
# ...
# 报告 ID: review_1779281588951
# 回复「确认 review_1779281588951 1,2」即可入库

# 2. 查看审查队列
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"审查队列"}'

# 输出：
# 待确认的审查报告 (1 个):
# 1. [05-20 20:53] review_1779281588951 (5 个候选)
#    1. 微内核架构决定 (score: 0.95)
#    ...

# 3. 确认入库
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"确认 review_1779281588951 1,3"}'

# 输出：已记录 2 条知识
```

**功能验证**：
- ✅ scan 精度：OR 匹配 7 个关键词
- ✅ 自然语言入口：审查一下、审查队列
- ✅ 文件持久化：`~/.hermes/reviews/`
- ✅ 确认流程：跨 session 可用
- ✅ scheduler 定时触发：每天 22:00

---

## 方向 A + B 实现：简化 UX + 通知闭环（21:15-21:30）

### 方向 A：简化确认 UX

新增快捷指令：

| 用户输入 | 动作 |
|---------|------|
| `确认全部` / `全部入库` | 入库最新报告的全部候选 |
| `跳过` / `清理审查` | 删除所有过期报告 |
| `确认 reviewID 1,2` | 选择性入库（已有） |

**实现**：
- 添加 `confirmAllTriggers` / `skipTriggers` 触发词
- 添加 `isConfirmAll` / `isSkipAll` 检测函数
- 添加 `handleConfirmAll` / `handleSkipAll` 处理函数

**文件**：`plugins/think_plugin.go`（+80行）

---

### 方向 B：通知闭环

在 `handleReviewExtract` 完成后异步发送通知。

**实现**：
```go
// 异步发送通知（方向 B：通知闭环）
go p.sendReviewNotification(reviewID, len(result.Candidates))
```

**通知配置**（环境变量）：
- `REVIEW_NOTIFY_CHANNEL`：通知渠道（email/slack/wechat）
- `REVIEW_NOTIFY_TARGET`：目标地址（webhook URL）

**文件**：`plugins/think_plugin.go`（+30行）

---

### 测试结果

```bash
# 1. 确认全部
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"确认全部"}'

# 输出：已入库 N 条知识

# 2. 跳过
curl -s http://localhost:8013/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"跳过"}'

# 输出：已清理 N 个审查报告
```

**功能验证**：
- ✅ 确认全部：一键入库最新报告
- ✅ 跳过：清理所有过期报告
- ✅ 通知闭环：审查完成后异步发送通知（需配置环境变量）

---

### 最终状态图

```
                    ┌─────────────────────────────────────────┐
                    │         Stage 3: Knowledge Review       │
                    ├─────────────────────────────────────────┤
                    │                                         │
  ✅ 已完成          │  scheduler 每天 22:00 自动触发           │
                    │  → scan（OR 匹配 7 个关键词）             │
                    │  → analyze（LLM 结构化提取）              │
                    │  → 报告写入 ~/.hermes/reviews/           │
                    │                                         │
                    │  用户："审查一下最近对话"                  │
                    │  → 触发工作流                             │
                    │                                         │
                    │  用户："审查队列"                         │
                    │  → 展示待确认报告                         │
                    │                                         │
                    │  用户："确认 reviewID 1,2"                │
                    │  → 入库 + 清理文件                        │
                    │                                         │
                    └─────────────────────────────────────────┘
```

**Stage 3 Knowledge Review Pipeline 已完整实现！**

---

## 知识关联与灵感：写入时 LLM + 检索时代码（21:30-22:00）

### 核心洞察：索引时 vs 检索时

```
Google 的架构：
  索引时（crawl time）：PageRank、语义分析、实体抽取 ← 离线、昂贵、用 ML
  查询时（query time）：倒排索引查找 + 排序         ← 在线、确定性、毫秒级

beishan 应该做的：
  写入时（index time）：LLM 提取关系类型、计算语义链接  ← 异步、昂贵、用 LLM
  检索时（query time）：图遍历 + 加权排序             ← 确定性代码、无 LLM
```

**这不违反宪法。** 宪法保护的是检索时的确定性，不禁止在写入时使用 LLM。

### 链接类型

```go
type LinkType string

const (
    LinkRelated     LinkType = "related"      // 相关
    LinkContradicts LinkType = "contradicts"   // 矛盾
    LinkSupersedes  LinkType = "supersedes"    // 替代/演进
    LinkSupports    LinkType = "supports"      // 支持/佐证
)
```

### 实现阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | TypedLink 结构体 + 存储 | ✅ |
| 1 | 增强 autoLinkEntry：LLM 语义建链 | ✅ |
| 2 | SearchMemory 图扩展按类型加权 | ✅ |
| 3 | FormatForPrompt serendipity slot | 🔲 |
| 4 | 矛盾标注渲染 | 🔲 |

### 实现细节

**阶段 0：TypedLink 结构体**
- 添加 `TypedLink` 结构体（TargetID + Type + Reason）
- KnowledgeEntry 添加 `TypedLinks` 字段（保留旧 `Links` 兼容）

**阶段 1：语义建链**
- `findSemanticLinks`：LLM 分析关系类型
- `callSemanticLinkLLM`：schema contract + validation gate
- `autoLinkEntry` 两层建链：确定性 + 语义

**阶段 2：类型加权**
- `loadLinkedEntries` 返回链接类型信息
- SearchMemory 图扩展按类型加权：
  - `contradicts`: 0.8（矛盾优先展示）
  - `supersedes`: 0.7（演进重要）
  - `related`: 0.5（默认）
  - `supports`: 0.3（佐证降权）

### 与宪法的对账

| 原则 | 是否违反 | 说明 |
|------|---------|------|
| 硬化层优先 | ✅ 不违反 | 链接类型是代码枚举，图遍历是代码 |
| 工具 > Agent | ✅ 不违反 | 检索时不调 LLM |
| LLM 只负责生成 | ⚠️ 灰色地带 | LLM 在写入时做语义分析，但产出是结构化数据 |
| 确定性检索 | ✅ 不违反 | 图遍历是纯代码，结果可复现 |
| 可审计 | ✅ 不违反 | 每个链接有 type + reason |

> **注意**：语义建链比 `expandKeywordsViaAPI` 更符合宪法——它把 LLM 调用从查询热路径移到了写入冷路径。

### 修改的文件

| 文件 | 改动 |
|------|------|
| `internal/tools/knowledge.go` | TypedLink 结构体、autoLinkEntry 增强、SearchMemory 加权 |

---

## 核心认知

1. **本地知识库的优势不是"智能"，而是精确的、个人的、确定性的事实。**

2. **自动写入比自动检索危险得多。** 错误知识会形成正反馈污染环。

3. **先做 autonomous discovery，不要立刻做 autonomous mutation。**

4. **vision 的价值是帮助拒绝错误方向，不是提前实现全部未来能力。**

5. **新抽象必须回答一个已发生的 bug。** 系统已经工作，不要进入"架构兴奋期"。

6. **修改 L1 前必须多次确认。** L1 是微内核，设计上应该冻结。

---

## Phase 2: Retrieval Contract Layer（检索契约层）

### 架构目标

将检索热路径从"LLM 优先"翻转为"确定性优先"，完全符合宪法：
- 查询时绝不调 LLM（删除 `expandKeywordsViaAPI`）
- 结构化加权在代码层完成
- 统一 `RetrievalResult` 契约为 Phase 3 多跳做准备

### Step 1: 翻转检索阶梯 + 删除 expandKeywordsViaAPI

**改动文件**: `internal/tools/knowledge.go`

**删除**: `expandKeywordsViaAPI` 函数（LLM at query time，违反宪法）

**新检索阶梯**:
```
L0:   确定性关键词评分（SearchWithScore，不调 LLM）
L0.5: 图扩展（沿 Links/TypedLinks 遍历）
L2:   embedding 语义回退（仅当 L0 结果不足时）
```

**新增类型**:
```go
type RetrievalKind string
const (
    KindDirect   RetrievalKind = "direct"   // L0 直接匹配
    KindLinked   RetrievalKind = "linked"   // L0.5 图扩展
    KindSemantic RetrievalKind = "semantic" // L2 语义回退
)

type RetrievalResult struct {
    Source   RetrievalKind
    Entry    *KnowledgeEntry
    Score    int
    LinkType LinkType
    LinkFrom string
}
```

**关键函数**:
- `searchMemoryFull`: 内部统一入口，返回 `[]RetrievalResult`
- `SearchMemory`: 外部接口，保持 `[]ScoredEntry` 兼容
- `linkTypeWeight`: 链接类型权重系数

### Step 2: 结构化加权（Structural Boost）

**加权规则**（在 `SearchWithScore` 中，代码层完成）:
| 条件 | 加分 | 原因 |
|------|------|------|
| 标签含 决策/决定/架构/方案/结论/教训/放弃 | +1 | 决策类知识优先 |
| 有 TypedLinks | +1 | 有关联的知识更重要 |
| 有 contradicts 链接 | +2 | 矛盾是灵感来源 |

**矛盾标注**:
- `ScoredEntry` 新增 `Contradictions []ContradictionAnnotation` 字段
- `FormatForPrompt` 渲染 `⚠️ 矛盾: "..." — reason` 标注

### Step 3: RetrievalResult Contract

统一 `RetrievalResult` 结构体，为 Phase 3 多跳检索做准备：
- `Source`: 检索来源（direct/linked/semantic）
- `LinkType`: 链接类型（仅 linked 来源）
- `LinkFrom`: 链接来源条目 ID
- `Contradictions`: 矛盾标注列表

**外部兼容**: `SearchMemory` 仍返回 `[]ScoredEntry`，不影响调用方。

### 验证

- `go build ./...` 通过
- `expandKeywordsViaAPI` 已完全删除，无残留引用
- `searchMemoryFull` 三个检索路径均填充 Contradictions

---

## Phase 3: Agentic Multi-hop Retrieval（多跳检索）

### 架构原则

**全程零 LLM 调用。** 多跳的"智能"来自图结构和代码规则，不来自 LLM 推断。

### Part A: 图深度扩展（knowledge.go）

**改动 1: supersedes 链式跟踪**
- `followSupersedesChain`: 沿 supersedes 链追溯历史版本，最多 3 跳，衰减 0.4
- 在图扩展阶段自动触发

**改动 2: 图扩展上限**
- `maxGraphExpand = 5`：单次图扩展最多返回 5 条，防爆炸

**改动 3: FormatForPromptFull**
- 带来源标注的格式化函数
- 标注：⚠️ 矛盾 / 📎 演进 / 🔗 关联 / 🔍 语义

**导出接口**:
```go
func SearchMemoryFull(query string, limit int, trace *RetrievalTrace) []RetrievalResult
func NeedsSecondHop(results []RetrievalResult) (bool, string)
func DeriveSecondQuery(results []RetrievalResult, reason string) string
func MergeResults(a, b []RetrievalResult) []RetrievalResult
func FormatForPromptFull(results []RetrievalResult) string
```

### Part B: think_plugin 多轮检索（think_plugin.go）

**触发条件**（三选一，纯代码判断）:
| 条件 | 原因 |
|------|------|
| 稀疏结果（只有 1 条直接命中） | 用 tags 展开查询 |
| 发现矛盾 | 拉更多上下文理解矛盾 |
| 发现演进链 | 拉最新版本 |

**第二轮查询构造**（纯代码）:
- sparse: 取 round 1 结果的 tags，去重取前 3 个
- contradiction: 用矛盾双方的标题关键词
- evolution: 用被替代条目的标题

**handleChat 改造**:
```
Round 1: SearchMemoryFull → 确定性检索
  ↓
NeedsSecondHop → 纯代码判断
  ↓ (yes)
DeriveSecondQuery → 纯代码构造
  ↓
Round 2: SearchMemoryFull → 合并 → FormatForPromptFull
```

### 改动汇总

| 文件 | 改动 |
|------|------|
| `internal/tools/knowledge.go` | `followSupersedesChain` + `maxGraphExpand` + 导出接口 + `FormatForPromptFull` |
| `plugins/think_plugin.go` | `handleChat` 多轮检索 |

### 验证

- `go build ./...` 通过
- 全程零 LLM 调用（多跳决策和查询构造都是纯代码）
- 合并后总结果不超过 5 条

---

## Phase 3 实测结果

### 测试环境
- 5 条种子数据 + 1 条图扩展专用条目 + 291 条现有知识库
- 端口临时改为 8015（8013 被 FangLab 占用）

### 测试 1: 图扩展 ✅
- 查询: "本地模型部署方案是什么"
- 结果: `stages=3`，`L0.5_graph` 成功找到 1 条通过 supersedes 链到达的条目
- kn_seed_5 →(supersedes)→ kn_seed_6（GPU 显存解决方案，标签无重叠）

### 测试 2: 矛盾信息传播 ✅
- 查询: "L1 架构冻结决定的详情"
- 结果: LLM 回复中正确引用了矛盾信息
- "有人将 L1 消息总线的超时机制从30秒改为120秒，这一做法与实际架构决策相矛盾"

### 测试 3: 多跳触发 ⚠️
- `NeedsSecondHop` 设计为保守触发（稀疏=仅1条直接命中）
- 当前测试数据标签高度重叠（都含"L1""架构"），L0 总是命中多条
- 多跳在真实使用中会触发：当用户查询非常具体、只命中 1 条时

### 关键发现
1. **图扩展在标签不重叠时正常工作** — supersedes 链成功拉出 GPU 显存条目
2. **矛盾信息被 LLM 正确使用** — 即使没有触发 multi_hop，图扩展已经把矛盾条目加入上下文
3. **291 条知识库是噪声源** — L0 总是命中 6 条（含 2 条无关的现有条目），导致 sparse 条件不触发

### 清理
- 移除 debug 日志
- 恢复端口为 8013
- `go build ./...` 通过
